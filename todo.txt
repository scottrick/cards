JSON-REPO WORK
======================================
-> update image urls to be the new url format as discussed in the github issue.
    -> MERGE THIS!

-> remove all references to the "Demo Deck" set and map them to "Virtual Premium Set".
    -> MERGE THIS!

-> Create CR with gemp Ids.
    -> spot check a bunch for both light/dark to make sure mapping is working correctly
    -> MERGE THIS!

-> handle first/second anthology cards.
    -> set -> SETS!!! Do it!!

-> Create new defensive shield set, and move all shields to the new set.
    -> do this after mapping to gemp ids, because otherwise the gemp file might not work anymore since the sets don't align
    -> update sets.json as well, and treat it just like set0 for now.

-> update card json sets to only have 1 name?  shorten the name in some cases

APP-WORK
======================================
-> update main activity to support landscape better; move tabs to the left-hand side when in landscape?

-> missing search functionality

-> Update SWCCG Card classes to match parser version.
    -> fall back to raw if necesssary

-> update build to make sure we can publish to existing playstore swccg app

-> configure proguard?  Do we need the consumer proguard file
    -> use R8 instead?

-> Make a real theme / pick material colors / pick a font / etc.
    -> Once we have the basic app working again, use these links to help get the design looking nice and sweet.
    -> https://material.io/design/color/the-color-system.html#tools-for-picking-colors
    -> https://material.io/resources/build-a-material-theme

-> dark mode support